<h1>Doses</h1>

<% @doses.each do |dose| %>

  <p><%= link_to dose.title, dose_path(dose) %> - <%= link_to "Delete", delete_dose_path(dose),
    method: :delete,
    data: { confirm: "Are you sure?" } %></p>
<% end %>
<br>
<br>
<p><%= link_to "Add a new dose", new_dose_path %></p>